{
  "name": "strauss-team",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/worker.test.js ../logic/logic.test.js",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
